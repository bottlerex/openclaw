# 跨 Agent 糾正記錄

所有 agent 每次會話必須讀取此檔案。
當 Rex 糾正任何 agent 的行為時，記錄在此。

## 永久規則（已確認的行為修正）

### 語言和格式
- 所有回覆用繁體中文
- 不用 emoji
- 簡短直接，不寫報告

### 操作行為
- 不要說「我無法做到」— 先讀 TOOLS.md 確認
- SSH 到 host 不算「外部操作」，不需要批准
- 刪除檔案前必須確認，用 trash 不用 rm

### 被否決的模式（不要再做）
[每次 Rex 否決某個行為時，加一條]

## 近期糾正（待確認是否永久化）
[新的糾正先放這裡，出現 2+ 次後移到永久規則]
